{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "model",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Model name",
      "x-prompt": "What is the name of the model?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "project": {
      "type": "string",
      "description": "Project name",
      "x-prompt": "What is the name of the project?"
    }
  },
  "required": [
    "name",
    "project"
  ]
}